// TODO: Update the logic to validate access token
const validateAccessToken = (accessToken: string): Promise<boolean> => {
  return new Promise((resolve, reject) => {
    resolve(true);
  });
};

export default {
  validateAccessToken
};
