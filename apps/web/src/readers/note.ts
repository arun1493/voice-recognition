import _property from "lodash/property";

const apptId = _property("apptId");
const compositionId = _property("compositionId");
const id = _property("id");
const submissionStatus = _property("submissionStatus");

export default {
  apptId,
  compositionId,
  id,
  submissionStatus
};
