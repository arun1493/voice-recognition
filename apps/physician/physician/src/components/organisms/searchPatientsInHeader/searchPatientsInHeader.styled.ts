import styled from "styled-components";
import { ModalContent } from "@suki-web/suki-ui";

const StyledModalContent = styled(ModalContent)`
  padding: 0;
  border-top: none;
`;

export { StyledModalContent };
