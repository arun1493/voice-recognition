import styled from "styled-components";

const StyledSukiLogoR = styled.div`
  color: ${({ theme }) => theme.pages.signUp.sukiLogoColor};
  cursor: pointer;
  height: 24px;
  width: 84px;
`;

export { StyledSukiLogoR };
